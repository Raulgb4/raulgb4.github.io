---
return Astro.redirect("/en/", 302);
---

<!--
  index.astro
  -----------
  Entry point for the root route ("/"). This page performs a server-side
  redirect to the default language version of the site, ensuring a
  consistent, language-prefixed URL structure.
-->
